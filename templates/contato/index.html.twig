{% extends 'base.html.twig' %}

{% block title %}Contato index{% endblock %}

{% block body %}
    <h1>Contato index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Telefone</th>
                <th>Celular</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for contato in contatos %}
            <tr>
                <td>{{ contato.id }}</td>
                <td>{{ contato.telefone }}</td>
                <td>{{ contato.celular }}</td>
                <td>
                    <a href="{{ path('contato_ver', {'id': contato.id}) }}">show</a>
                    <a href="{{ path('contato_editar', {'id': contato.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('contato_novo') }}">Create new</a>
{% endblock %}
