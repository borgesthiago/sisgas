{% form_theme form 'form/form_div.html.twig' %}
<div class="ui grid">
    <div class="column">
        {{ form_start(form, {'attr': {'class': 'ui form'}}) }}
        <div class="fields">
            {{ form_row(form.email, {'attr': {'data-row-class': 'seven wide'}}) }}
            {{ form_row(form.password, {'attr': {'data-row-class': 'four wide'}}) }}
        </div>
        <div class="fields">           
            {{ form_row(form.funcionario, {'attr': {'data-row-class': 'four wide'}}) }}
            {{ form_row(form.roles, {'attr': {'data-row-class': 'six wide'}}) }}
        </div>
       
        <div class="fields">
            <div class="ui right floated buttons">
                <button class="ui button">{{ button_label|default('Salvar') }}</button>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}