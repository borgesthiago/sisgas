{% extends 'base.html.twig' %}

{% block title %}Servicos{% endblock %}

{% block body %}
    <h1>Servicos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ servico.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ servico.nome }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ servico.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('servicos_index') }}">back to list</a>

    <a href="{{ path('servicos_edit', {'id': servico.id}) }}">edit</a>

    {{ include('servicos/_delete_form.html.twig') }}
{% endblock %}
